node_modules/
openapi2code

# Real-world specs fetched for benchmarking. github.json (~12 MB) and
# stripe.json (~7.7 MB) are excluded as too large to commit -- see
# "Reproducing this benchmark" in README.md for the download commands.
# petstore-v3.json (~17 KB) and azure-translator-v2.json (~64 KB) are small
# enough to keep, similar to large-synthetic.json.
specs/github.json
specs/stripe.json
